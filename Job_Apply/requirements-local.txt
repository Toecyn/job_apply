# Full local dev install: pip install -r requirements-local.txt
# python-jobspy now lives in requirements.txt itself — the Vercel-deployed
# dashboard scrapes on demand via the Refresh Jobs button. This file adds
# only what a standalone scheduler.py process needs on top of that
# (apscheduler for its own cron loop) plus the MCP server.
-r requirements.txt
apscheduler
mcp
